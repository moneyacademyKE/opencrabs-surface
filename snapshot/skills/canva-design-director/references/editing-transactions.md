# Editing Transactions

When operating the MCP, batch your visual updates.
Do not make 10 separate requests. Start a transaction, stack the modifications (changing text string, updating font size, realigning bounding boxes), and commit. Precision and efficiency are key.
