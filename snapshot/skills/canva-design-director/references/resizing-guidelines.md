# Resizing Guidelines

When using the `resize-design` tool:
1. An automated resize rarely produces a perfect layout.
2. Immediately follow up with a search of the new design, inspect element coordinates, and begin an editing transaction to fix alignment constraints.
